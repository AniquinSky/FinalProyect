<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>One Piece Fan Page</title>
    <style>
        :root {
            --primary-color: #B22222; /* Rojo One Piece */
            --secondary-color: #1E3A8A; /* Azul marino */
            --accent-color: #FFD700; /* Oro del sombrero de paja */
            --text-light: #ffffff;
            --text-dark: #333333;
            --bg-transparent: rgba(255, 255, 255, 0.92);
            --spacing-medium: 20px;
            --spacing-large: 40px;
        }
        
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Roboto, Arial, sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            background-image: url('https://images.unsplash.com/photo-1633613286848-e6f43bbafb8d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1470&q=80');
            background-size: cover;
            background-attachment: fixed;
            background-position: center;
            min-height: 100vh;
        }

        .container {
            width: 90%;
            max-width: 1200px;
            margin: 30px auto;
            background-color: var(--bg-transparent);
            padding: var(--spacing-medium);
            box-shadow: 0 0 25px rgba(0, 0, 0, 0.15);
            border-radius: 10px;
        }

        header {
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: var(--text-light);
            padding: 15px 25px;
            border-radius: 8px;
            margin-bottom: var(--spacing-large);
            background-image: url('https://i.imgur.com/0pXJZzX.png');
            background-size: cover;
            position: relative;
        }

        header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(178, 34, 34, 0.7), rgba(30, 58, 138, 0.7));
            border-radius: 8px;
        }

        .logo {
            height: 70px;
            width: 70px;
            margin-right: 25px;
            background-color: var(--text-light);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            font-weight: bold;
            color: var(--primary-color);
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
            position: relative;
            z-index: 1;
            background-image: url('https://i.imgur.com/JQJZQyQ.png');
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
        }

        h1 {
            margin: 0;
            font-size: 2.2rem;
            letter-spacing: 0.5px;
            position: relative;
            z-index: 1;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        h2 {
            color: var(--primary-color);
            border-bottom: 2px solid var(--accent-color);
            padding-bottom: 10px;
            margin-top: 30px;
        }

        main {
            padding: 0 15px;
        }

        .character-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: var(--spacing-medium);
            margin: var(--spacing-large) 0;
        }

        .character-card {
            background-color: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
            border: 2px solid var(--primary-color);
        }

        .character-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .character-image {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-bottom: 3px solid var(--accent-color);
        }

        .character-info {
            padding: 15px;
        }

        .character-name {
            color: var(--primary-color);
            margin: 0 0 10px 0;
            font-size: 1.4rem;
        }

        .character-detail {
            margin: 8px 0;
            font-size: 0.95rem;
        }

        .character-detail strong {
            color: var(--secondary-color);
        }

        .buttons {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }

        .btn {
            padding: 8px 15px;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            flex: 1;
        }

        .btn-edit {
            background-color: var(--accent-color);
            color: var(--text-dark);
        }

        .btn-edit:hover {
            background-color: #e6c200;
        }

        .btn-delete {
            background-color: var(--primary-color);
            color: white;
        }

        .btn-delete:hover {
            background-color: #8B0000;
        }

        .btn-add {
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: bold;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            display: inline-block;
            margin-bottom: var(--spacing-medium);
            text-decoration: none;
        }

        .btn-add:hover {
            background: linear-gradient(to right, var(--secondary-color), var(--primary-color));
            transform: translateY(-3px);
            box-shadow: 0 7px 15px rgba(0, 0, 0, 0.2);
        }

        footer {
            margin-top: var(--spacing-large);
            background: linear-gradient(135deg, var(--secondary-color), #444);
            color: var(--text-light);
            padding: 25px;
            font-size: 0.95rem;
            border-radius: 8px;
            text-align: center;
        }

        footer p {
            margin: 8px 0;
            line-height: 1.8;
        }

        .highlight {
            color: var(--accent-color);
            font-weight: bold;
        }

        .video-container {
            position: relative;
            padding-bottom: 56.25%; /* 16:9 */
            height: 0;
            overflow: hidden;
            margin: 20px 0;
            border-radius: 8px;
            border: 3px solid var(--accent-color);
        }

        .video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        @media (max-width: 768px) {
            header {
                flex-direction: column;
                text-align: center;
                padding: 20px;
            }
            
            .logo {
                margin-right: 0;
                margin-bottom: 15px;
                height: 60px;
                width: 60px;
                font-size: 2rem;
            }
            
            .container {
                width: 95%;
                padding: 15px;
            }
            
            h1 {
                font-size: 1.8rem;
            }

            .character-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo"></div>
            <h1>⛵ Personajes de <span class="highlight">One Piece</span></h1>
        </header>

        <main>
            <a href="/add" class="btn-add">➕ Agregar personaje</a>
            
            <div class="character-grid">
                <% personajes.forEach(p => { %>
                <div class="character-card">
                    <img src="<%= p.imagen_url %>" class="character-image" alt="<%= p.nombre %>">
                    <div class="character-info">
                        <h3 class="character-name"><%= p.nombre %></h3>
                        <p class="character-detail"><strong>Fruta del Diablo:</strong> <%= p.fruta_del_diablo %></p>
                        <p class="character-detail"><strong>Recompensa:</strong> <%= p.recompensa %></p>
                        
                        <% if(p.video_url) { %>
                        <div class="video-container">
                            <iframe src="<%= p.video_url %>" frameborder="0" allowfullscreen></iframe>
                        </div>
                        <% } %>
                        
                        <div class="buttons">
                            <a href="/edit/<%= p.id %>" class="btn btn-edit">Editar</a>
                            <form action="/delete/<%= p.id %>" method="POST" style="display: inline;">
                                <button type="submit" class="btn btn-delete" onclick="return confirm('¿Eliminar este personaje?')">Eliminar</button>
                            </form>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </main>

        <footer>
            <p><strong>Fan Page de One Piece</strong></p>
            <p>El viaje para encontrar el legendario tesoro del Rey de los Piratas</p>
            <p>¡Sueña y arriésgalo todo por ello!</p>
        </footer>
    </div>
</body>
</html>
