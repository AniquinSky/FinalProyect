
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Formulario Personaje</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<div class="container py-4">
    <h1 class="mb-4"><%= personaje.id ? "Editar" : "Agregar" %> personaje</h1>
    <form action="<%= action %>" method="POST">
        <div class="mb-3">
            <label>Nombre:</label>
            <input type="text" name="nombre" value="<%= personaje.nombre || '' %>" class="form-control" required>
        </div>
        <div class="mb-3">
            <label>Fruta del Diablo:</label>
            <input type="text" name="fruta_del_diablo" value="<%= personaje.fruta_del_diablo || '' %>" class="form-control">
        </div>
        <div class="mb-3">
            <label>Recompensa:</label>
            <input type="text" name="recompensa" value="<%= personaje.recompensa || '' %>" class="form-control">
        </div>
        <div class="mb-3">
            <label>Imagen URL:</label>
            <input type="url" name="imagen_url" value="<%= personaje.imagen_url || '' %>" class="form-control">
        </div>
        <div class="mb-3">
            <label>Video URL (YouTube embebido):</label>
            <input type="url" name="video_url" value="<%= personaje.video_url || '' %>" class="form-control">
        </div>
        <button class="btn btn-primary">Guardar</button>
        <a href="/" class="btn btn-secondary">Cancelar</a>
    </form>
</div>
</body>
</html>
